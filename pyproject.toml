[build-system]
requires = ["setuptools>=56", "wheel", "setuptools_scm[toml]>=10.0.5"]
build-backend = "setuptools.build_meta"

[tool.black]
skip-string-normalization = false

[tool.setuptools_scm]

[tool.pytest-enabler.mypy]
addopts = "--mypy"

[tool.pytest-enabler.cov]
addopts = "--cov"
